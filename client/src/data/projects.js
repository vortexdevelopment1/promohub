/**
 * Projects data is strictly managed dynamically through the Admin Video Upload system & MongoDB database via GET /api/videos.
 * Static/dummy/mock video data has been removed.
 */
export const projects = [];
export default projects;
